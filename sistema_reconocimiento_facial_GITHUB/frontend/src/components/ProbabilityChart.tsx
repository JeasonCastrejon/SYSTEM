import {LineChart,Line,XAxis,YAxis,CartesianGrid,Tooltip,ResponsiveContainer} from "recharts";
export default function ProbabilityChart({value}:{value:number|null}){if(value==null)return <div className="grid h-72 place-items-center rounded-xl bg-slate-50 text-sm text-slate-400">El gráfico aparecerá con una predicción real.</div>;const data=[{name:"Probabilidad",valor:Number((value*100).toFixed(2))}];return <div className="h-72"><ResponsiveContainer width="100%" height="100%"><LineChart data={data}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="name"/><YAxis domain={[0,100]} tickFormatter={v=>`${v}%`}/><Tooltip formatter={(v)=>`${v}%`}/><Line type="monotone" dataKey="valor" strokeWidth={3} dot={{r:5}}/></LineChart></ResponsiveContainer></div>}
