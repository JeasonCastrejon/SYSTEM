export default function SimilarityBar({value,threshold}:{value:number;threshold:number}){const p=Math.max(0,Math.min(1,value))*100;const t=Math.max(0,Math.min(1,threshold))*100;return <div><div className="mb-2 flex justify-between text-xs font-semibold"><span>Similitud</span><span>{p.toFixed(1)}%</span></div><div className="relative h-2.5 rounded-full bg-slate-100"><div className="h-full rounded-full bg-blue-600" style={{width:`${p}%`}}/><div className="absolute -top-1 h-4 w-0.5 bg-slate-900" style={{left:`${t}%`}}/></div><div className="mt-2 flex justify-between text-[11px] text-slate-400"><span>0%</span><span>Umbral {(threshold*100).toFixed(0)}%</span><span>100%</span></div></div>}
