import {useEffect,useState} from "react";import {Clock3} from "lucide-react";import PageHeader from "../components/PageHeader";import {historial} from "../services/api";
export default function Historial(){const[data,setData]=useState<any[]>([]);useEffect(()=>{historial().then(setData).catch(()=>{})},[]);return <div><PageHeader title="Historial y auditoría" description="Consulta intentos, identidad candidata, similitud, distancia, umbral y probabilidad calibrada."/><div className="card overflow-hidden"><div className="border-b border-slate-200 p-5"><h2 className="font-bold">Registros reales</h2></div>{!data.length?<div className="grid min-h-56 place-items-center p-8 text-center text-slate-400"><div><Clock3 className="mx-auto mb-3" size={38}/><p className="font-semibold">Sin registros todavía</p><p className="mt-1 text-sm">No se muestran datos ficticios.</p></div></div>:<div className="overflow-auto"><table className="w-full min-w-[850px] text-sm"><thead className="bg-slate-50"><tr>{["Fecha","Persona","Similitud","Distancia","Umbral","Coincide","Probabilidad"].map(x=><th className="p-3 text-left" key={x}>{x}</th>)}</tr></thead><tbody>{data.map((r:any)=><tr className="border-t" key={r.id}><td className="p-3">{new Date(r.created_at).toLocaleString()}</td><td className="p-3">{r.nombre??"No identificada"}</td><td className="p-3">{(r.similitud*100).toFixed(1)}%</td><td className="p-3">{Number(r.distancia).toFixed(4)}</td><td className="p-3">{(r.umbral*100).toFixed(1)}%</td><td className="p-3">{r.coincide?"Sí":"No"}</td><td className="p-3">{r.probabilidad_calibrada==null?"—":`${(r.probabilidad_calibrada*100).toFixed(1)}%`}</td></tr>)}</tbody></table></div>}</div></div>}
